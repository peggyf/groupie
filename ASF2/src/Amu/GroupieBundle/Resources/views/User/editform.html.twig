{% extends 'AppBundle::page.html.twig' %}

{% block titre %}Droits sur les groupes{% endblock %}

{% block body %}

    <form action="{{ path('user_update')}}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}

    <input type="submit"  value="Rechercher" />
    </form>
    
{% endblock %}